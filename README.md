Ejemplo de colas de mensajes con Kafka.
El servicio productor envía un mensaje al topic 'test_topic' y el consumidor lo recibe y lo imprime.

El servicio productor esta hecho en PHP (Laravel) y el consumidor en Python (Django).

Para ejecutar el proyecto:
1. Clonar el repositorio
2. Ejecutar docker-compose up -d
